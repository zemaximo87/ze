name: deployon:
  push:
    branches:
      - masterjobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12'
      - name: setup git auth
        env:
          email: 'torkpuppet@gmail.com'
          username: 'zemaximo87'
          access_token: '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'
        run: |
          git config --global credential.helper store
          echo "https://${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}:x-oauth-basic@github.com" > ~/.git-credentials
          git config --global user.email $email
          git config --global user.name $username
      - name: deploy to production
        run: |
          npm i
          NODE_ENV=production npm run build
          cd dist
          git init
          git add -A
          git commit -m "🚀 Deploy id ${GITHUB_RUN_ID}"
          git push -f "https://github.com/${zemaximo87/ze}.git" master:production